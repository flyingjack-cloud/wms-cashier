<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.flyingjack.cashier.mapper.CategoryMapper">

    <select id="findByParentId" resultType="top.flyingjack.cashier.entity.Category">
        SELECT id, group_id, parent_id, name
        FROM wms_category
        WHERE group_id = #{groupId} AND parent_id = #{parentId}
    </select>

    <select id="findById" resultType="top.flyingjack.cashier.entity.Category">
        SELECT id, group_id, parent_id, name FROM wms_category WHERE id = #{id} AND group_id = #{groupId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wms_category (group_id, parent_id, name)
        VALUES (#{groupId}, #{parentId}, #{name})
    </insert>

    <delete id="deleteById">
        DELETE FROM wms_category WHERE id = #{id} AND group_id = #{groupId}
    </delete>

</mapper>
