<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.flyingjack.cashier.mapper.NoticeMapper">

    <select id="findLatestByGroupAndType" resultType="top.flyingjack.cashier.entity.Notice">
        SELECT id, group_id, type, content, created_at
        FROM wms_notice
        WHERE group_id = #{groupId} AND type = #{type}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

</mapper>
