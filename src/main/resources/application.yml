server:
  port: 8081

logging:
  level:
    org:
      springframework:
        security: DEBUG
        web: DEBUG
spring:
  application:
    name: wms-cashier
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000