FROM eclipse-temurin:21-jre-alpine
LABEL authors="znjac"

WORKDIR /app

# Maven produces one executable .jar; the .jar.original backup does not match.
COPY target/*.jar /app/app.jar

ENTRYPOINT ["java", "-jar", "/app/app.jar"]
